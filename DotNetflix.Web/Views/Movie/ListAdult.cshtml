@model MovieListViewModel

<h1>Movies</h1>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Year</th>
            <th>Genres</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var movie in Model.Movies)
        {

            <tr>
                <td><a asp-controller="Movie" asp-action="MovieInfo" asp-route-movieid="@movie.Id">@movie.Title</a></td>
                <td>@movie.Year</td>
                <td>
                    @{
                        int nrGenres = movie.Genres.Count();
                        for (int i = 0; i < nrGenres; i++)
                        {
                            @movie.Genres[i];
                            if (i + 1 < nrGenres)
                            { <span> | </span> }
                        }
                    }
                </td>
                <td>
                        <a asp-controller="UserMovie" asp-action="AddToWishlist" asp-route-movieId="@movie.Id" class="btn btn-outline-success">Add to wishlist</a>
                </td>
                <td>
                        <a asp-controller="Movie" asp-action="RateMovie" asp-route-movieId="@movie.Id" class="btn btn-outline-primary">Rate Movie</a>                    
                </td>
            </tr>

        }

    </tbody>
</table>